# 2026-07-22

## 项目分析（礼往来 LiWangLai）
- 完成全项目结构分析：SwiftUI + SwiftData 人情礼簿 iOS App，51 个 Swift 文件，零第三方依赖，XcodeGen（project.yml）生成工程。
- 数据层：GiftRecord + HostedGiftEvent 两个 @Model，版本化 Schema；UserDefaults 存主题/生物锁。
- 服务层 11 个 Service：记录、场次、提醒、备份、Excel 导入导出、去重、通知、生物识别、StoreKit 内购。
- 测试：Swift Testing，9 个文件约 96-100 用例，含内存容器隔离测试；scheme 强制串行（SWT_EXPERIMENTAL_MAXIMUM_PARALLELIZATION_WIDTH=1）。
- 现状注意点：工作区有 20+ 文件未提交修改（iPad 礼台相关）；project.yml 版本 1.1(build 6) 与 git log 中 v1.5 提交信息不一致。
- 未完成：iCloud 同步、真机验收、App Store 上架、隐私政策 URL。

## 逻辑审查（三路并行 agent 完成）
- 严重 bug：① RecordService.swift:216 金额 `Int(filter(\.isNumber))` 吞小数点（100.5→10050）；② PurchaseManager 权益明文 UserDefaults 可绕过内购；③ EventDetailView/PersonDetailView 值快照删除记录后 body 重算可能 trap；④ ExcelImportService:239 超大数 Int 强转 trap。
- 设计矛盾：needsReturn 要求 returnReminderDate≠nil，未设提醒收礼永不进待回（ReminderService 有死代码）；首页提醒卡算全部年份 vs 年度卡只算当年，口径打架。
- 数据一致性：AddRecordView 改日期静默清 hostedEventID；HostedEventService 同步依赖调用方传入完整数组；人物聚合联系方式只留数字致误合并；姓名规范化三套口径（SearchService/PeopleView/AddRecord 不统一）；Excel 导出缺"场次"列，导出再导入丢归属。
- 功能缺失：礼台收礼不产生回礼提醒、缺场次收送对比、缺年度对比/金额排序、逾期提醒无标识。
